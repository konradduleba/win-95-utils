import { Meta, IconGallery, IconItem } from "@storybook/addon-docs/blocks";

import { ICON_LIST } from "./mocks";

<Meta title="System/Icons" />

# Icons

<IconGallery>
  {ICON_LIST.map((Icon) => (
    <IconItem key={Icon.displayName} name={Icon.displayName}>
      <Icon />
    </IconItem>
  ))}
</IconGallery>
