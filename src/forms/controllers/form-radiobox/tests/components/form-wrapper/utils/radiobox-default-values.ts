import { TEST_NAME } from "../../../mocks";

import { FALLBACK_VALUE } from "./consts";
import { FormValues } from "./types";

export const DEFAULT_VALUES: FormValues = {
  [TEST_NAME]: FALLBACK_VALUE,
};
